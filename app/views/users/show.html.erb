<div class="container">
  <%= render '/urls/form' %>
</div>

<div class="container">
  <ul>
    <% @urls.each do |url| %>
      <li>
        <%= link_to "unpretentio.us/#{url.uid}", "/#{url.uid}" %>
      </li>
    <% end %>
  </ul>
</div>
